Implement `is_balanced(s)` in `pkg/core.py` so the tests pass. Run them with
`python3 tests/test_visible.py`.

`is_balanced` returns True iff the brackets `()[]{}` in `s` are balanced and
correctly nested (order matters — `([)]` is NOT balanced). Non-bracket characters
are ignored. Do not change the signature. Keep the module importable.
