Implement `roman_to_int(s)` in `pkg/core.py` so the tests pass. Run them with
`python3 tests/test_visible.py`.

A Roman numeral maps I=1, V=5, X=10, L=50, C=100, D=500, M=1000, and uses
subtractive notation (e.g. IV=4, IX=9, XL=40, XC=90, CD=400, CM=900). Do not change
the signature. Keep the module importable.
