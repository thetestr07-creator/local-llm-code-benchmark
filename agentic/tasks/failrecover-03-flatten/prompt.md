Implement `flatten(lst)` in `pkg/core.py` so the tests pass. Run them with
`python3 tests/test_visible.py`.

`flatten` returns a flat list of every non-list element, in order, recursing to any
depth of nesting. Only Python `list` values are treated as nestable; other values
(ints, strings, tuples, etc.) are kept as-is. Do not change the signature. Keep the
module importable.
