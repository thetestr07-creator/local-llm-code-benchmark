[project]
name = "intervalset"
version = "0.1.0"
description = "A normalized set of half-open [start, end) intervals."
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
