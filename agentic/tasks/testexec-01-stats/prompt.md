Implement the stubbed functions `mean` and `median` in `statskit/core.py` so that
the test suite passes. Run the tests with `python3 tests/test_stats.py` to check
your work.

Requirements:
- `mean(xs)` returns the arithmetic mean of a non-empty list.
- `median(xs)` returns the median; the input is not guaranteed to be sorted, and
  for an even-length list it is the average of the two middle values.

Do not change the function signatures. Keep the module importable.
